export enum Language {
  English = "en",
  Turkish = "tr",
}

export enum Pages {
  Home = "home",
  Login = "login",
}

export enum SidebarIds {
  Home = 1,
  TicketManagement,
  CarManagement,
}

export enum FlightDirection {
  OneWay = "oneWay",
  RoundTrip = "roundTrip",
}
